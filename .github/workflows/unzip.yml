 permissions:
     contents: write
   ```

   Here’s the full updated file:

   ```yaml
   name: Unzip Uploaded Project

   on:
     workflow_dispatch:

   permissions:
     contents: write

   jobs:
     unzip:
       runs-on: ubuntu-latest

       steps:
         - name: Checkout repository
           uses: actions/checkout@v4

         - name: Install unzip
           run: sudo apt-get install unzip

         - name: Unzip project
           run: |
             unzip -o *.zip
             rm *.zip

         - name: Commit changes
           run: |
             git config user.name "github-actions"
             git config user.email "actions@github.com"
             git add .
             git commit -m "Unzip project files" || echo "No changes to commit"

         - name: Push changes
           run: |
             git push
   ```
