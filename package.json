{
  "name": "elif-college-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix Backend",
    "install": "npm install --prefix Backend",
    "postinstall": "cd Backend && npm install"
  }
}
